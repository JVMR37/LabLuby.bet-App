import { DefaultTheme } from "react-native-paper";

export const appTheme = {
  ...DefaultTheme,
  roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: "#b5c401",
    accent: "#f1c40f",
  },
};
